<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="../ColorsAndGeneralSettings/Colors.xaml"/>
    </ResourceDictionary.MergedDictionaries>

    <!-- TextBlock -->

    <Style x:Key="DefaultTextBlock" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource WindowForeground }" />
        <Setter Property="FontSize" Value="13" />
        <Setter Property="Margin" Value="10" />
    </Style>

    <Style x:Key="Header" TargetType="TextBlock" BasedOn ="{StaticResource DefaultTextBlock}">
        <Setter Property="Foreground" Value="{StaticResource WindowHeaderForeground }" />
        <Setter Property="FontSize" Value="15" />
        <Setter Property="FontWeight" Value="SemiBold" />
    </Style>

    <Style x:Key="BrightTextBlock" TargetType="TextBlock" BasedOn ="{StaticResource DefaultTextBlock}">
        <Setter Property="Foreground" Value="{StaticResource WindowBrightHeaderBackground }" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="FontWeight" Value="SemiBold" />
    </Style>

</ResourceDictionary>